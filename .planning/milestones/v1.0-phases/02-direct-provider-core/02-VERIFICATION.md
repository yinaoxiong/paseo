---
phase: 02-direct-provider-core
verified: 2026-06-13T12:40:54Z
status: passed
score: 18/18 must-haves verified
overrides_applied: 0
---

# Phase 2: Direct Provider Core Verification Report

**Phase Goal:** Add the experimental `cursor-sdk` direct provider with local session lifecycle, persistence, cancellation, and Sandbox/YOLO mode handling.
**Verified:** 2026-06-13T12:40:54Z
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                      | Status   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Paseo can create a local Cursor SDK session through a direct provider class.                                                                                               | VERIFIED | `CursorSdkAgentClient` implements `AgentClient`, registry wires `"cursor-sdk"` to it, and `createSession()` builds a JSONL store then calls the runtime `createAgent()` with Cursor SDK local options.                                                                                                                                                                                                                      |
| 2   | Submitted user prompts create canonical Paseo user timeline rows.                                                                                                          | VERIFIED | `startTurn()` emits `turn_started` and exactly one `timeline` item of `type: "user_message"` using the provided message id or a generated `cursor-sdk-user-*` id before SDK stream processing. Tests assert SDK user echoes do not add duplicates.                                                                                                                                                                          |
| 3   | SDK stream output becomes Paseo assistant, tool, reasoning, usage, and completion events where available.                                                                  | VERIFIED | `run.stream()` is pumped through `mapCursorSdkStreamEvent()` for assistant, reasoning, tool, status/request/run diagnostics, and unknown events. `run.wait()` maps terminal status through `mapCursorSdkTerminalStatus()`. Phase 1 evidence documented assistant/thinking/tool/status/request/terminal surfaces, with usage only "where SDK provides it"; no stable usage field is used by the implemented Phase 02 mapper. |
| 4   | The persistence handle is sufficient to resume a Cursor SDK session after daemon restart.                                                                                  | VERIFIED | `describePersistence()` writes provider, session id, SDK agent id in `nativeHandle`, and strict secret-free metadata. `resumeSession()` parses that handle, validates store containment, recreates the JSONL store, and calls `resumeAgent(parsed.sdkAgentId, options)`.                                                                                                                                                    |
| 5   | Sandbox and YOLO map to explicit SDK runtime options and YOLO is marked unattended.                                                                                        | VERIFIED | Mode helpers define Sandbox as `isUnattended: false` and YOLO as `isUnattended: true`. Create/resume passes `local.sandboxOptions.enabled` from the selected mode, and unsupported Sandbox fails before SDK calls.                                                                                                                                                                                                          |
| 6   | Provider snapshots can include an experimental cursor-sdk provider without replacing existing cursor ACP behavior.                                                         | VERIFIED | `provider-registry.ts` imports `CursorSdkAgentClient` and registers a separate `"cursor-sdk"` factory while preserving the existing `cursor` ACP factory. Registry tests assert both providers coexist.                                                                                                                                                                                                                     |
| 7   | Missing `CURSOR_API_KEY` makes cursor-sdk unavailable with a redacted diagnostic and no scratch SDK session.                                                               | VERIFIED | `isAvailable()` checks readiness and key presence only; tests use the fake runtime to assert no `createAgent`, `resumeAgent`, or `listModels` calls on missing key.                                                                                                                                                                                                                                                         |
| 8   | Runtime settings key resolution prefers `agents.providers.cursor-sdk.env.CURSOR_API_KEY` over process env.                                                                 | VERIFIED | `resolveApiKey()` checks `runtimeSettings.env.CURSOR_API_KEY` before `process.env.CURSOR_API_KEY`; tests assert provider-config wins and only key source metadata is exposed.                                                                                                                                                                                                                                               |
| 9   | Sandbox is listed only when side-effect-free runtime support says it is available, and YOLO is explicitly unattended/dangerous.                                            | VERIFIED | `listModes()` delegates to `checkSandboxSupport()` and `listCursorSdkModes()`. Production support currently reports unsupported without creating sessions; tests cover both supported and unsupported mode lists.                                                                                                                                                                                                           |
| 10  | Creating a cursor-sdk session uses a provider/session-scoped `JsonlLocalAgentStore` under `$PASEO_HOME/providers/cursor-sdk/stores/{paseoSessionId}`.                      | VERIFIED | `buildCursorSdkStorePath()` derives that path, `assertCursorSdkStorePath()` validates it, and `ProductionCursorSdkRuntime.createJsonlStore()` creates a `JsonlLocalAgentStore` there.                                                                                                                                                                                                                                       |
| 11  | Resuming a cursor-sdk session validates `nativeHandle`, `cwd`, `runtime`, `storePath`, `modeId`, `sandboxEnabled`, and optional model before calling the SDK.              | VERIFIED | `CursorSdkResumeMetadataSchema` is strict and `parseCursorSdkPersistenceHandle()` requires provider, safe session id, native handle, metadata fields, and path containment.                                                                                                                                                                                                                                                 |
| 12  | Sandbox create/resume requests fail closed when sandbox support is unavailable; YOLO is used only for explicit YOLO/unattended intent.                                     | VERIFIED | `assertModeSupported()` throws for Sandbox when support is false; create/resume call it before SDK calls. YOLO maps directly to `sandboxEnabled: false`.                                                                                                                                                                                                                                                                    |
| 13  | Resume overrides for model or mode are accepted only when the requested SDK operation succeeds; failures emit diagnostics without fallback.                                | VERIFIED | `resumeSession()` applies explicit override values into SDK options, and catch blocks throw redacted SDK resume errors without falling back to persisted mode/model. Tests cover override failure.                                                                                                                                                                                                                          |
| 14  | Interrupt uses the active SDK Run lifecycle and maps supported cancellation to a canceled turn path.                                                                       | VERIFIED | `interrupt()` waits for `activeRun`, checks `supports("cancel")`, calls `cancel()` only when supported, and terminal `cancelled` maps to `turn_canceled`.                                                                                                                                                                                                                                                                   |
| 15  | Each accepted foreground prompt emits exactly one canonical Paseo `user_message` row from `startTurn`, not SDK echoes.                                                     | VERIFIED | The mapper returns diagnostics only for SDK `user` events; session tests assert a single `user_message` row for a prompt.                                                                                                                                                                                                                                                                                                   |
| 16  | Observed SDK assistant, reasoning/thinking, tool_call, status, runId, and requestId surfaces map into Paseo stream events or redacted diagnostics without crashing a turn. | VERIFIED | `event-mapper.ts` has permissive guards for stream events and tests for assistant, thinking, tool, status/correlation, malformed, and unknown events.                                                                                                                                                                                                                                                                       |
| 17  | Terminal statuses map directly: finished -> `turn_completed`, cancelled -> `turn_canceled`, error or unknown -> `turn_failed` with diagnostics.                            | VERIFIED | `mapCursorSdkTerminalStatus()` implements those mappings and tests cover finished, cancelled, error, and unknown `paused` status.                                                                                                                                                                                                                                                                                           |
| 18  | SDK tool calls are classified only when reliable; unknown tools remain generic or diagnostic.                                                                              | VERIFIED | `mapCursorSdkToolCall()` requires call id/name, derives known tool details only from reliable shapes, and otherwise emits `detail.type: "unknown"` with sanitized payload summaries.                                                                                                                                                                                                                                        |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact                                                                | Expected                                     | Status   | Details                                                                                                                                                        |
| ----------------------------------------------------------------------- | -------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/server/src/server/agent/providers/cursor-sdk-agent.ts`        | Direct provider client and session lifecycle | VERIFIED | Exports `CursorSdkAgentClient`; implements create/resume/run/startTurn/interrupt/close and imports runtime, modes, persistence, mapper, and `runProviderTurn`. |
| `packages/server/src/server/agent/providers/cursor-sdk/sdk-runtime.ts`  | Injectable SDK adapter                       | VERIFIED | Defines runtime port and production `@cursor/sdk` import adapter for readiness, models, JSONL store, `Agent.create`, and `Agent.resume`.                       |
| `packages/server/src/server/agent/providers/cursor-sdk/diagnostics.ts`  | Redacted diagnostics                         | VERIFIED | Redacts configured secrets, key-shaped strings, authorization, headers, and env in formatted diagnostics.                                                      |
| `packages/server/src/server/agent/providers/cursor-sdk/modes.ts`        | Sandbox/YOLO mode helpers                    | VERIFIED | Defines only Sandbox and YOLO and gates Sandbox on side-effect-free support.                                                                                   |
| `packages/server/src/server/agent/providers/cursor-sdk/persistence.ts`  | Strict persistence                           | VERIFIED | Strict zod schema, safe session ids, controlled store root, symlink/path containment, and secret-free handle creation/parsing.                                 |
| `packages/server/src/server/agent/providers/cursor-sdk/event-mapper.ts` | SDK stream mapper                            | VERIFIED | Maps assistant/reasoning/tool/terminal events and redacted diagnostics for status, user echoes, and unknown events.                                            |
| `packages/server/src/server/agent/provider-registry.ts`                 | Built-in factory                             | VERIFIED | Imports `CursorSdkAgentClient` and registers `"cursor-sdk"` separately from `cursor`.                                                                          |
| `packages/protocol/src/provider-manifest.ts`                            | Provider metadata                            | VERIFIED | Defines `cursor-sdk`, Sandbox, and YOLO with YOLO `isUnattended: true`.                                                                                        |
| Targeted tests                                                          | Provider behavior coverage                   | VERIFIED | Test files cover registry, provider lifecycle, persistence, and event mapping; reported final devcontainer run passed 4 files / 86 tests.                      |

### Key Link Verification

| From                   | To                          | Via                                  | Status | Details                                                                                                                                                           |
| ---------------------- | --------------------------- | ------------------------------------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `provider-registry.ts` | `cursor-sdk-agent.ts`       | `"cursor-sdk"` factory               | WIRED  | Import exists and factory constructs `new CursorSdkAgentClient({ logger, runtimeSettings })`. The automated regex missed this because the expression spans lines. |
| `cursor-sdk-agent.ts`  | `provider-launch-config.ts` | `ProviderRuntimeSettings.env`        | WIRED  | Client accepts `runtimeSettings` and resolves `CURSOR_API_KEY` from `runtimeSettings.env` before process env.                                                     |
| `cursor-sdk-agent.ts`  | `modes.ts`                  | `listModes`, create/resume preflight | WIRED  | Imports and calls `listCursorSdkModes()` and `resolveCursorSdkMode()`; create/resume call `assertModeSupported()`.                                                |
| `cursor-sdk-agent.ts`  | `persistence.ts`            | create/resume/describePersistence    | WIRED  | Imports `buildCursorSdkStorePath`, `assertCursorSdkStorePath`, `createCursorSdkPersistenceHandle`, and `parseCursorSdkPersistenceHandle`.                         |
| `cursor-sdk-agent.ts`  | `@cursor/sdk`               | `ProductionCursorSdkRuntime`         | WIRED  | Provider imports the runtime adapter; runtime adapter calls `JsonlLocalAgentStore`, `Agent.create`, and `Agent.resume`.                                           |
| `cursor-sdk-agent.ts`  | SDK Run cancellation        | `interrupt()`                        | WIRED  | `interrupt()` checks `activeRun.supports("cancel")`, calls `activeRun.cancel()`, and terminal mapping emits `turn_canceled`.                                      |
| `cursor-sdk-agent.ts`  | `event-mapper.ts`           | SDK stream pump                      | WIRED  | `pumpRun()` calls `mapCursorSdkStreamEvent()` for stream chunks and `mapCursorSdkTerminalStatus()` for `run.wait()`.                                              |
| `event-mapper.ts`      | `agent-sdk-types.ts`        | normalized outputs                   | WIRED  | Mapper imports `AgentStreamEvent`, `ToolCallDetail`, and `ToolCallTimelineItem`.                                                                                  |
| `cursor-sdk-agent.ts`  | `provider-runner.ts`        | `run()`                              | WIRED  | `run()` delegates to `runProviderTurn()` so callers receive final text and normalized timeline.                                                                   |

### Data-Flow Trace (Level 4)

| Artifact              | Data Variable                    | Source                                                    | Produces Real Data | Status                                                                                           |
| --------------------- | -------------------------------- | --------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------ |
| `cursor-sdk-agent.ts` | Prompt text / canonical user row | `startTurn(prompt, options)`                              | Yes                | FLOWING - prompt is rendered to text, emitted as `user_message`, then sent to SDK.               |
| `cursor-sdk-agent.ts` | SDK stream events                | `sdkAgent.send()` -> `run.stream()`                       | Yes                | FLOWING - stream chunks pass through `mapCursorSdkStreamEvent()` and are emitted to subscribers. |
| `cursor-sdk-agent.ts` | Terminal lifecycle               | `run.wait()`                                              | Yes                | FLOWING - terminal result maps to completed/canceled/failed and clears active run state.         |
| `persistence.ts`      | Resume metadata                  | `describePersistence()` / stored `AgentPersistenceHandle` | Yes                | FLOWING - parsed handle feeds `resumeSession()` and `runtime.resumeAgent()`.                     |
| `modes.ts`            | Sandbox/YOLO runtime option      | selected `modeId`                                         | Yes                | FLOWING - create/resume convert mode id to `local.sandboxOptions.enabled`.                       |

### Behavioral Spot-Checks

| Behavior                                                         | Command                                                                                                                                                                                                                                                                          | Result                                                                                                                                                                                                   | Status |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Targeted provider, registry, persistence, and event mapper tests | `npm run test:unit --workspace=@getpaseo/server -- src/server/agent/provider-registry.test.ts src/server/agent/providers/cursor-sdk-agent.test.ts src/server/agent/providers/cursor-sdk/persistence.test.ts src/server/agent/providers/cursor-sdk/event-mapper.test.ts --bail=1` | Reported by orchestrator as passed: 4 files, 86 tests. Not rerun on host because dependency-backed checks must run in the devcontainer and project rules say not to rerun already-green targeted suites. | PASS   |
| Typecheck                                                        | `npm run typecheck` in devcontainer                                                                                                                                                                                                                                              | Reported by orchestrator as passed after final source changes.                                                                                                                                           | PASS   |
| Lint/format hooks                                                | commit hooks plus repeated lint/format checks in devcontainer                                                                                                                                                                                                                    | Reported by orchestrator as passed.                                                                                                                                                                      | PASS   |

### Probe Execution

| Probe | Command                                                      | Result                       | Status  |
| ----- | ------------------------------------------------------------ | ---------------------------- | ------- |
| None  | Probe discovery found no declared `probe-*.sh` for Phase 02. | No probe execution required. | SKIPPED |

### Requirements Coverage

| Requirement | Source Plan  | Description                                                               | Status    | Evidence                                                                                                                              |
| ----------- | ------------ | ------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| PROV-01     | 02-01        | Experimental direct provider id `cursor-sdk`                              | SATISFIED | Registry factory and protocol manifest define `cursor-sdk`; tests assert coexistence with `cursor`.                                   |
| PROV-02     | 02-01, 02-02 | Implements `AgentClient` and `AgentSession` without ACP process transport | SATISFIED | `CursorSdkAgentClient` and internal `CursorSdkAgentSession` are direct provider classes using SDK runtime adapter, not ACP transport. |
| PROV-03     | 02-03        | Canonical user timeline and SDK assistant/tool/reasoning mapping          | SATISFIED | `startTurn()` emits canonical `user_message`; mapper covers assistant, reasoning, and tool calls.                                     |
| PROV-04     | 02-02        | Resume handle can recreate a Cursor SDK agent after daemon restart        | SATISFIED | Handle stores SDK id in `nativeHandle`; resume parses handle and calls `resumeAgent()` with controlled store path.                    |
| PROV-05     | 02-02, 02-03 | Interruption/cancellation using SDK run lifecycle                         | SATISFIED | `interrupt()` uses SDK run cancel support; terminal cancelled maps to `turn_canceled`.                                                |
| MODE-01     | 02-01, 02-02 | Sandbox mode runs local agents with sandboxing enabled                    | SATISFIED | Sandbox mode maps to `sandboxOptions.enabled: true`; unsupported runtime fails closed before SDK create/resume.                       |
| MODE-02     | 02-01, 02-02 | YOLO mode disables sandbox and is marked unattended/dangerous             | SATISFIED | YOLO maps to `sandboxOptions.enabled: false` and `isUnattended: true` in helpers and manifest.                                        |

### Anti-Patterns Found

| File                                         | Line | Pattern                                     | Severity | Impact                                                                                                                                                         |
| -------------------------------------------- | ---: | ------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/protocol/src/provider-manifest.ts` |   21 | Existing `TODO` about static provider modes | Warning  | Non-blocking existing design debt; Phase 02 did not rely on it for runtime mode truth because `CursorSdkAgentClient.listModes()` provides runtime-gated modes. |

### Human Verification Required

None. Phase 02 is provider-core infrastructure and every roadmap success criterion was verified through source wiring and focused tests. Live Cursor SDK auth and live Sandbox execution remain optional environmental checks, not Phase 02 blockers.

### Gaps Summary

No gaps found. Phase goal achieved and ready to proceed.

---

_Verified: 2026-06-13T12:40:54Z_
_Verifier: the agent (gsd-verifier)_
